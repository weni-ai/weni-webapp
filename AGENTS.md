Use como conteúdo completo do `AGENTS.md` quando o arquivo não existir ainda (mantenha o `# AGENTS.md` abaixo). Quando o arquivo já existir, **não** copie o `# AGENTS.md` — apenas anexe o bloco entre os marcadores `<!-- localization-lock:begin -->` / `<!-- localization-lock:end -->` ao final do arquivo existente, preservando todo o conteúdo anterior.

---

# AGENTS.md

<!-- localization-lock:begin -->
Always write UI strings in the source language only — do not translate them
to other languages. The Localization team manages all translations through Crowdin.

To know which files are which: check crowdin.yml. The source field identifies
files you may write to. Any file matching the translation patterns is a
Crowdin-managed translation — do not create, modify, or delete it.
<!-- localization-lock:end -->
