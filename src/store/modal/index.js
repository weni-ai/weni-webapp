import actions from './actions';
import mutations from './mutations';

const state = {
  lastId: 0,
  actives: [],
};

const getters = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
