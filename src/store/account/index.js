import actions from './actions';
import mutations from './mutations';

const state = {
  loading: null,
  profile: null,
};

const getters = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
